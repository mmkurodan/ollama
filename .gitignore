app/src/main/assets/*.gguf

# NPU/Hexagon removed from the public repo to avoid Qualcomm Hexagon SDK
# (Community Version) redistribution restrictions. Kept locally only; full
# backup at /root/llama-npu-backup/ (git bundle + tarballs). 2026-06.
app/src/main/cpp/hexagon-artifacts/
app/src/main/jniLibs/arm64-v8a/libggml-htp-*.so
vendor/hexagon/
.github/workflows/build-hexagon-artifacts.yml
scripts/*hexagon*
/LICENSE AGREEMENT.txt

# ローカル専用ツール (PAT 等の機密を含むためコミットしない)
gha_log_analyzer.sh

.gradle/
gradle/
gradlew
gradlew.bat
*.iml
.idea/
local.properties
build/
/release.keystore
/release.keystore.base64

# build-local.sh が CI から取得する再利用 .so（CIの externalNativeBuild と重複するためコミットしない）
/app/src/main/jniLibs/arm64-v8a/libllama_jni.so
/app/src/main/jniLibs/arm64-v8a/libggml-opencl.so
